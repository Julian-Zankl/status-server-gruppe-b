services:
  status-server:
    image: status-server
    ports:
      - "8080:80"
    build:
      context: ./Backend

  status-client:
    image: status-client
    ports:
      - "4200:80"
    depends_on:
      status-server:
        condition: service_started
    build:
      context: ./Frontend